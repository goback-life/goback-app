name: dedecube_custom_lints
description: "Dedecube Custom Lints provides a collection of custom lint rules
  tailored for Flutter applications"
publish_to: "none"

version: 0.0.4

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  analyzer: ^7.3.0
  analyzer_plugin: ^0.13.0
  custom_lint_builder: ^0.7.5
  yaml: ^3.1.3

dev_dependencies:
  flutter_lints: ^5.0.0
